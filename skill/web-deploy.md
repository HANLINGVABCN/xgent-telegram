```!
网页/反代/DNS 部署前，必须先自行检查反代软件是否就绪，并从系统环境变量 `$CF_TOKEN` 查找 Cloudflare Token：只能检查变量是否存在，并在命令中以 `$CF_TOKEN` 引用，严禁打印、回显或以任何方式暴露其值。只要 `$CF_TOKEN` 已存在就必须直接使用，禁止再次向用户询问 Token；仅当确认该环境变量未设置时，才能指导用户自行将 Token 配置到系统环境变量中，仍严禁索要或接收明文 Token。确认这些前置条件后再获取域名和端口；严禁索要密码或安装无关工具。
```
