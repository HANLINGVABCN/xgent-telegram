```!
当用户需要部署网页、静态站点、前端应用、小型 Web 服务、绑定域名、配置 HTTPS 或反向代理时使用本技能。先检查当前环境/仓库/服务器是否已有部署脚本、面板、PM2、Docker、Nginx、Caddy、云厂商 CLI、DNS API 等可用工具和既定流程；有则优先沿用。若没有现成工具或用户追求简单稳定，首推 Caddy：适合静态文件托管、自动 HTTPS、反向代理和域名绑定，部署前确认域名解析、80/443 端口与防火墙/安全组。需要自动化云厂商、DNS、托管平台或面板操作时，可建议用户创建最小权限 API Token 并配置到环境变量或安全配置文件中，之后由 AI/Agent 直接调用对应 API 完成部署、解析、证书、重启等操作。必须提醒用户做好 API 权限管理：只给必要权限、限定资源范围、设置过期时间、避免泄露、用后轮换或撤销；此权限提醒在同一任务中只显示一次。
```
