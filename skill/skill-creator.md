```!
Skill 制作指南：在 `skill/` 下新建 `<名称>.md` 文件。仓库公共 skill 放 `skill/` 根目录；仅本部署实例使用的私有 skill 放 `skill/private/`（该目录被 git 忽略、更新永不被覆盖）。每个 skill 可在 TG /skills 菜单或 web 设置面板里单独启用/禁用，默认全部启用。
核心铁律：只有 ` ```! ` 简介块的内容会被系统自动加载进 Prompt 上下文！
对于简单的、或者必须时刻记住的技能与功能，直接把核心配置和用法简短精炼地全部写在 ` ```! ` 简介块里即可！如需进一步解释，再在下面写在正文中，非必要不加正文。
```

# Skill 制作规范与指南

## 1. 核心原理
- **上下文加载**：只有文件最开头的 ` ```! ` 围栏块会被系统自动扫描并拼接入 Prompt 上下文。
- **正文按需读取**：正文不会占用日常上下文 Token，只有遇到非常复杂的长流程时才需要写正文供按需读取。
- **按需启停**：每个 skill 默认启用。不需要的可在 TG 的 `/skills` 菜单或 web 设置面板「Skill 管理」中关闭，关掉的不进 Prompt。

## 2. 编写法则
- **简单 / 必备技能**：把环境变量、核心命令和安全规范直接简短精炼地塞进 ` ```! ` 简介块里，光写个简介块即可。
- **复杂技能**：简介块写核心索引，正文中放置长篇排查手册或复杂机制。
- **路径用相对路径**：命令与脚本路径一律相对项目根目录（如 `skill/script/xxx/run.sh`），不要写死 `/home/用户名/...`，否则换机器即失效。

## 3. 简介块范例

好的简介块（简洁、含命令、含安全提示）：

```
便签技能：一键部署一个轻量私有网页便签。功能包括账号密码登录、便签列表、新建/编辑/删除、标题与正文搜索。安装: sudo bash skill/script/notes/install.sh install。更新: sudo bash skill/script/notes/install.sh update。卸载: sudo bash skill/script/notes/install.sh uninstall。默认端口 8899，部署时交互填写端口、账号、密码。
```

要点：一句话说清功能 → 三条核心命令 → 关键默认值。不展开细节，细节放正文。

## 4. 目录规范
- 仓库公共技能文件：`skill/<技能名称>.md`
- 私有技能文件（本实例自定义，不进仓库、更新不覆盖）：`skill/private/<技能名称>.md`
- 配套脚本（可选）：`skill/script/<技能名称>/`

