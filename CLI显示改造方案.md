# CLI 显示改动要求

只改 CLI 前端显示，只许改格式和符号，不许动别的。没提到的一律不动。

## 要求

1. **只把 AI 的回复和我的提问框上**，其他一律不加框。
2. AI/用户消息的框**不要左右包边**，改成**上下两条横线**：标题嵌在上横线里（`── ❯ User ────` / `── ◆ XGent ────`），正文夹在两条横线之间。
   原因：带左右边的框，一拉终端窗口就全乱。
3. **命令返回**：不加横线不加框；人物从 XGent 改成 **`◇ 命令`**；字号小一号；正文**整行灰色底填充**。
4. **系统提示**：人物改成 **`◇ 系统`**；字号小一号；无底色。
5. **tokens 统计**：不要标题不要框，纯小字一行。
6. **启动 banner**：也改成双横线。
7. `/help`、`/` 命令面板、`/getchat` 的输出：按命令返回样式（◇ 命令 + 小字 + 灰底）。
8. **输入框、提示符（❯ 及三档颜色）、菜单编号、键位提示、配色体系不动。**
9. 排版必须扛住终端窗口拉宽/拉窄，不允许出现错位乱码。

## 样式示例

```
── ❯ User ──────────────────────────
  你好
─────────────────────────────────────

── ◆ XGent ──────────────────────────
  AI 回复正文
─────────────────────────────────────

◇ 命令
  命令输出（小字 + 整行灰底）

◇ 系统
  系统提示（小字，无底）

  ↑ 21825 tokens (0 cached) · ↓ 1958 tokens · ⚡ 43 tokens/s
```
