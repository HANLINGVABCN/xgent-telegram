# 交接文档（2026-08-22 会话末）

给下一个会话：本文档列出**仍未解决的问题**（含全部线索与怀疑方向）、本会话已完成的内容、以及排查所需的关键事实。代码全部已推送（最新 `302748b`），工作区干净。

## 部署与约定事实

- 服务器：cloudcone，项目目录 `/home/hanling/xgent-telegram`，PM2 应用名 `xgent-telegram`（id 3），另有 webdav-filemanager（id 0）
- 仓库：`HANLINGVABCN/xgent-telegram-test`（私有），服务器从它拉取；`git pull` 后必须 `pm2 restart xgent-telegram` 才生效（启动日志会打印"代码版本: vXxx"）
- **测试一律在 WSL 里跑**（用户的规矩）：`wsl.exe -e bash -lc "cd /mnt/c/.../xgent-telegram-test && python3 -m pytest tests/ -q"`
- sections 改动后必须 `python tools/check_split_integrity.py --write` 更新基线
- 用户主要用 Telegram bot（人格"洛溪"）、Web 界面、CLI（服务器上 `xgent` 命令）三端
- install.sh 有"IP 出站模式： 仅 IPv4"选项——**服务器网络到 Telegram 可能需要强制 IPv4**（见问题 2）

## 仍未解决的问题（按优先级）

### 问题 1：Web 刷屏 + 吞消息（CLI 发消息时触发）——未确诊

- 症状：用户在 **CLI 发消息**后，Web 界面"一直刷屏"，且吞消息。用户在 Web 端发消息也报告过同样现象（上一条消息无限重复）。
- 已排除/已修但未愈的假设：
  - ~~edit 帧 id 错位（fake/real id）~~ —— `302748b` 已修（MirrorBot 反向映射 + 回归测试），**用户反馈仍在刷屏**
  - ~~edit 帧无 id 每次新建~~ —— `e37c035` 已加 lastStreamEntry 复用，未愈
  - ~~刷新后历史显示协议块/提示词原文~~ —— `b5604e6` 已修（AGENT_STATUS 落库 + AGENT_CMD 显示层隐藏）
- 剩余头号嫌疑：**`_web_external_record_watcher`（idle.py）**——CLI 进程写库的记录被它转成 SSE 帧推给网页。CLI 一轮 Agent 对话产生多类记录（USER_TEXT/AGENT_RESULT/AGENT_STATUS/AI_REPLY/TOKEN_USAGE/MEDIA_REPLY），每 2 秒一批。用户看到的"刷屏"很可能是这些帧的某种重复或前端处理异常。**关键排查动作：**
  1. 浏览器 F12 → Network → EventSource（/api/stream）→ 看重复的是哪种帧（message/edit/user_message/notice）、message_id 是什么、间隔多少
  2. F12 Console 红字
  3. 复现时 `pm2 logs xgent-telegram --lines 50 --nostream`
- 次要嫌疑：SSE 断线重连循环（每次 onopen 触发 resync 重拉历史）；`_web_read_history`/`upgradeBubbleMedia` 异步回调跨重载（`e37c035` 加了 historyGen 代次，未验证是否足够）

### 问题 2：CLI→Telegram 镜像极慢 + 吞消息——强嫌疑：IPv6

- 实现：`xgent_cli.py` 的 `_tg_send_message` 用 urllib 直 POST `{API_BASE_URL 或 https://api.telegram.org}/bot<token>/sendMessage`
- 已改为 create_task 后台发送（`302748b`），CLI 本身不再卡，但**用户反馈同步仍极慢且吞**
- 强嫌疑：服务器出站 IPv6 到 Telegram 不通/极慢（install.sh 专门有"仅 IPv4"模式就是这个原因），urllib 默认先试 IPv6 → 挂到超时。**修法方向：`_tg_send_message` 里强制 IPv4（自定义 opener/monkeypatch socket.getaddrinfo 只回 IPv4，与 install.sh 的出站模式对齐），或读 .env 的出站模式配置**
- 另注意吞消息 = 发送失败只记 logger.warning，不重试

### 问题 3：未验证项（低优先）

- CLI 面板 `read_line` 正常提交补 `_finish()`（`e37c035`）——修了"消息显示两遍"，但无 pty 自动化测试覆盖
- `/sync` 拉取历史中 AGENT_STATUS 行的显示（应显示状态行，未实测）
- 老版本入库的坏数据（text_markdown 转义时代的 `\_` 记录、旧 /export 无路径记录）不会自愈

## 明确没做的（之前提过/答应过）

1. **CLI OSC 8 可点击路径**：📎 卡片路径渲染成终端超链接（需要动 cli_render 的 display_width 忽略 OSC 序列，独立小工程）
2. **纯 Web 模式上传放宽到 2GB**：需先把 `_read_multipart` 从一次性读内存改成流式落盘
3. **网页端 CLI 消息的来源标识**：外部帧（CLI 写库转推）目前无"🖥 CLI"角标，与刷新后的显示无法区分来源
4. **CLI 面板 pty 自动化测试**：wrap 修复、滚动条、三档提示符都只有纯逻辑测试，无真终端回归

## 本会话已完成（全部已推送，测试 428 过）

- **CLI**：长输入折行疯狂复制 bug；面板滚动到底部+淡蓝高亮+右侧滚动条；三档提示符（红=输入态 Ctrl+C 取消 / 黄=菜单 Ctrl+C 关闭 / 绿=空闲退出）；用户消息成块（❯ user）保留；/restart 从 CLI 发起不再误报无 PM2 并退出；/sync [N] 拉取跨端历史；banner 版本号；提交后不再显示两遍
- **导出/媒体**：/export 落盘到 xgent_storage/exports/ 并把路径写进 AI 可见的系统消息；Web 媒体刷新恢复（/api/media/resolve 白名单端点 + 历史文本挖路径）；图片内联/视频音频播放器/文件卡片，下载一律按钮
- **Telegram 入站文本**：三轮修复后最终为**原文逐字节直传**（text_markdown 转义损坏 API Key 的根因；富粘贴混合消息；最终放弃一切格式重构）
- **三端同步**：CLI→TG 镜像（用户消息+AI 回复）；CLI→Web 实时（DB 观察者，2s 轮询 rowid 游标，metadata.src 防双显）；Web↔Bot 原有镜像保留
- **Web 显示**：TG HTML 换行修复（/status）；命令补全自动选中；AGENT_STATUS 轮次状态落库、刷新与实时流一致；AGENT_CMD 协议原文显示层隐藏；token 行灰条；贴底才自动滚动
- **AI 上下文**：命令记录加 `[命令]` 前缀（防相邻误读，用户被 /restart 误判那次）；AGENT_STATUS/TOKEN_USAGE 不进上下文
- **提示词**：agent_addon.txt 前部示例块删除（168 行），铁律补"不支持 toolcall、禁止调用工具"；服务端启动日志打印代码版本

## 下个对话开工建议

先让用户提供问题 1 的三样排查材料（EventSource 帧流截图/F12 Console/pm2 logs），同时可直接动手问题 2（IPv4 强制，改动小、收益明确）。
